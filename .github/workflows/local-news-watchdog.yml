name: Yongin Mohyeon Newsletter Watchdog

on:
  schedule:
    # Korea time 08:13 and 09:13. Morning catch-up for yesterday if every evening run was missed.
    - cron: '13 23 * * *'
    - cron: '13 0 * * *'
    # Korea time 20:13, 21:13, 22:13, 23:13. Independent backup for missed scheduled runs.
    - cron: '13 11 * * *'
    - cron: '13 12 * * *'
    - cron: '13 13 * * *'
    - cron: '13 14 * * *'
  workflow_run:
    workflows:
      - Apptech Kakao Alert
    types:
      - completed
  workflow_dispatch:

permissions:
  contents: write

defaults:
  run:
    shell: bash

jobs:
  send-if-missing:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check whether local newsletter is missing
        id: guard
        run: |
          today=$(TZ=Asia/Seoul date +%F)
          yesterday=$(TZ=Asia/Seoul date -d 'yesterday' +%F)
          hour=$(TZ=Asia/Seoul date +%H)
          echo "today=$today" >> "$GITHUB_OUTPUT"

          target_kind="today"
          target_date="$today"
          target_env=""
          page_url="https://jypark-821108.github.io/apptech-kakao-alert/newsletter.html"

          if [ "$hour" -lt 18 ]; then
            target_kind="yesterday"
            target_date="$yesterday"
            target_env="$yesterday"
            page_url="https://jypark-821108.github.io/apptech-kakao-alert/news/$yesterday.html"
          fi

          echo "target_date=$target_date" >> "$GITHUB_OUTPUT"
          echo "target_env=$target_env" >> "$GITHUB_OUTPUT"
          echo "page_url=$page_url" >> "$GITHUB_OUTPUT"

          status=$(python - <<PY
          import json, pathlib, re
          target = '$target_date'
          status_path = pathlib.Path('local-news-status.json')
          data_path = pathlib.Path('docs/news-data') / f'{target}.json'
          try:
              status = json.loads(status_path.read_text(encoding='utf-8'))
          except Exception:
              status = {}
          try:
              data = json.loads(data_path.read_text(encoding='utf-8'))
              article_count = len(data.get('articles') or [])
          except Exception:
              article_count = 0
          sent_date = str(status.get('date') or '')
          sent_at = str(status.get('sent_at') or '')
          m = re.search(r'\b(\d{2}):(\d{2}):(\d{2})\b', sent_at)
          sent_hour = int(m.group(1)) if m else -1
          print(f'{sent_date},{article_count},{sent_hour}')
          PY
          )
          sent_date=$(echo "$status" | cut -d, -f1)
          article_count=$(echo "$status" | cut -d, -f2)
          sent_hour=$(echo "$status" | cut -d, -f3)

          if [ "$target_kind" = "today" ]; then
            if [ "$sent_date" = "$today" ] && [ "$article_count" -gt 0 ] && [ "$sent_hour" -ge 18 ]; then
              echo "send=false" >> "$GITHUB_OUTPUT"
              echo "Today's local newsletter already exists and was sent after 18:00 KST."
            else
              echo "send=true" >> "$GITHUB_OUTPUT"
              echo "Today's local newsletter is missing or only has an early/empty run. Watchdog will send."
            fi
          else
            if [ "$sent_date" = "$yesterday" ] && [ "$article_count" -gt 0 ]; then
              echo "send=false" >> "$GITHUB_OUTPUT"
              echo "Yesterday's local newsletter already exists. Morning catch-up is not needed."
            else
              echo "send=true" >> "$GITHUB_OUTPUT"
              echo "Yesterday's local newsletter is missing. Morning catch-up will send the archived date link."
            fi
          fi
      - uses: actions/setup-python@v5
        if: steps.guard.outputs.send == 'true'
        with:
          python-version: '3.12'
      - run: pip install -r requirements.txt
        if: steps.guard.outputs.send == 'true'
      - name: Build and send local newsletter
        if: steps.guard.outputs.send == 'true'
        env:
          KAKAO_REST_API_KEY: ${{ secrets.KAKAO_REST_API_KEY }}
          KAKAO_REFRESH_TOKEN: ${{ secrets.KAKAO_REFRESH_TOKEN }}
          NEWSLETTER_URL: ${{ steps.guard.outputs.page_url }}
          TARGET_DATE: ${{ steps.guard.outputs.target_env }}
        run: python local_newsletter.py
      - name: Mark newsletter sent
        if: steps.guard.outputs.send == 'true'
        run: |
          target_date="${{ steps.guard.outputs.target_date }}"
          article_count=$(python - <<PY
          import json, pathlib
          path = pathlib.Path('docs/news-data') / '$target_date.json'
          try:
              data = json.loads(path.read_text(encoding='utf-8'))
              print(len(data.get('articles') or []))
          except Exception:
              print(0)
          PY
          )
          cat > local-news-status.json <<EOF
          {
            "date": "$target_date",
            "sent_at": "$(TZ=Asia/Seoul date '+%Y-%m-%d %H:%M:%S %Z')",
            "article_count": $article_count,
            "source": "watchdog"
          }
          EOF
      - name: Commit newsletter page
        if: steps.guard.outputs.send == 'true' && always()
        run: |
          if [ -f docs/newsletter.html ] || [ -f local-news-status.json ]; then
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add docs/newsletter.html docs/news docs/news-data local-news-status.json
            git diff --cached --quiet || git commit -m "Update local newsletter page from watchdog"
            git push
          fi
